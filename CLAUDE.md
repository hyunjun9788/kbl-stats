# Project Instructions

프로젝트의 기술 스택과 관련 규칙은 아래 문서를 따른다.

@stack.md

## Git Branch Rules

- `main` 브랜치에서 직접 작업하거나 커밋하지 않는다.
- 작업을 시작하기 전에 현재 브랜치와 작업 트리의 변경 사항을 확인한다.
- 새로운 작업은 목적에 맞는 별도 브랜치를 생성해서 진행한다.
- 브랜치 이름은 짧고 명확한 영문 kebab-case로 작성하며 다음 형식을 사용한다.
  - 기능: `feature/<작업명>`
  - 버그 수정: `fix/<작업명>`
  - 리팩터링: `refactor/<작업명>`
  - 문서: `docs/<작업명>`
  - 설정 및 유지보수: `chore/<작업명>`
- 사용자가 명시적으로 요청하지 않는 한 브랜치를 병합하거나 삭제하지 않는다.
- `push`, `merge`, `rebase`, `reset`, force push는 실행 전에 사용자에게 확인한다.
- 기존의 커밋되지 않은 변경 사항을 덮어쓰거나 되돌리지 않는다.
- 커밋하기 전에 변경 내용을 검토하고 관련 테스트를 실행한다.
